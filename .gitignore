venv/
cache/
__pycache__/
*.pyc

# Real company sales data and local test artifacts - never goes to GitHub,
# even in a private repo. data/ reaches the server separately (see
# DEPLOYMENT.md, Part B step 2) via rsync, not via git.
data/
test_uploads/
scratch/

# Local secrets/config and this coding tool's own session files - never
# part of the project itself.
.env
.env.local
.claude/
